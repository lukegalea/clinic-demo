[
  # Injected by `use AshBpmn.Web.DesignerLive` at the `use` site (line 7):
  # a pattern in the macro-generated code can never match nil in this host's
  # configuration. The wrapper is the same three-line shape the VPM spike
  # mounts; the finding belongs to the framework's expansion, not to this
  # app's code. Track it upstream rather than working around it here.
  {"lib/clinic_demo_web/live/bpmn/designer_live.ex:7:pattern_match",
   "The pattern can never match the type nil."}
]
